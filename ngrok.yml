version: "2"
tunnels:
  chat-app:
    proto: http
    addr: 3001
    inspect: false
    host_header: rewrite
    schemes:
      - https
    headers:
      - Access-Control-Allow-Origin: http://localhost:3000
      - Access-Control-Allow-Credentials: true